import { AccountTicketsView } from 'src/sections/account/tickets/account-tickets-view';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Account: Contact Support',
};

export default function AccountTicketsPage() {
    return <AccountTicketsView />;
}
